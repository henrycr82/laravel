<!-- $errors es una variable global que se incluye en los view de laravel donde se almacenan los mensajes de error -->
@if ($errors->any())
	<div class="alert alert-danger">
		<ul>
			@foreach ($errors->all() as $error)
				<li>{{ $error }}</li>
			@endforeach
		</ul>
	</div>
@endif